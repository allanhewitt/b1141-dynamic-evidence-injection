const app=document.querySelector('#app');const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const api=async(url,opts={})=>{const r=await fetch(url,{headers:{'content-type':'application/json',...(opts.headers||{})},...opts});const b=await r.json();if(!r.ok)throw new Error(b.error||'Request failed');return b};const page=(html,mode='')=>{document.body.className=mode;app.innerHTML=`<div class="shell">${html}</div>`};const route=()=>{const [p,q='']=location.hash.slice(2).split('?');return{parts:p.split('/').filter(Boolean),params:new URLSearchParams(q)}};const token=()=>{let t=sessionStorage.getItem('dei-token');if(!t){t=crypto.randomUUID();sessionStorage.setItem('dei-token',t)}return t};const scale=(labels,selected)=>`<div class="scale">${labels.map((l,i)=>`<button data-v="${i+1}" class="${selected===i+1?'selected':''}">${esc(l)}</button>`).join('')}</div>`;
async function resolveSession(activity,id){if(id)return id;const d=await api(`/api/dei/activities/${encodeURIComponent(activity)}/open-session`);if(!d.session_id)throw new Error('This activity is not open yet.');return d.session_id}
function flow(matrix,labels,focus=null){const rows=matrix.map((r,i)=>focus&&focus!==i+1?r.map(()=>0):r);const pairs=[];rows.forEach((r,i)=>r.forEach((n,j)=>{if(n)pairs.push(`${esc(labels[i])} → ${esc(labels[j])}: ${n}`)}));return `<div class="panel"><strong>Response movement</strong><ul>${pairs.length?pairs.map(x=>`<li>${x}</li>`).join(''):'<li>No completed responses yet</li>'}</ul></div>`}
async function student(activityId,params){const activity=await api(`/api/dei/activities/${encodeURIComponent(activityId)}`),sessionId=await resolveSession(activityId,params.get('session')),pt=token(),s={step:1,initial:null,revised:null,release:null,result:null};const render=()=>{if(s.step===1){page(`<div class="eyebrow">Your view</div><h1>What do you think?</h1><p class="muted">${esc(activity.response.instruction_initial)}</p><div class="prompt">${esc(activity.response.prompt)}</div>${scale(activity.response.scale_labels,s.initial)}<div class="actions"><button id="next" class="button primary" ${s.initial?'':'disabled'}>Continue</button></div>`);document.querySelectorAll('[data-v]').forEach(b=>b.onclick=()=>{s.initial=+b.dataset.v;render()});document.querySelector('#next').onclick=async()=>{s.release=await api(`/api/dei/sessions/${sessionId}/initial`,{method:'POST',body:JSON.stringify({participant_token:pt,response:s.initial})});s.step=2;render()}}else if(s.step===2){page(`<div class="eyebrow">New information</div><h1>Review the following information</h1><p class="muted">${esc(s.release.review.instruction)}</p>${s.release.information.map(x=>`<article class="info">${x.title?`<h3>${esc(x.title)}</h3>`:''}<p>${esc(x.body)}</p>${x.source_note?`<div class="source">${esc(x.source_note)}</div>`:''}</article>`).join('')}<button id="reviewed" class="button primary">${esc(s.release.review.continue_label)}</button>`);document.querySelector('#reviewed').onclick=async()=>{await api(`/api/dei/sessions/${sessionId}/reviewed`,{method:'POST',body:JSON.stringify({participant_token:pt})});s.step=3;render()}}else if(s.step===3){page(`<div class="eyebrow">Your view now</div><h1>What do you think now?</h1><p class="muted">${esc(activity.response.instruction_revised)}</p><div class="prompt">${esc(activity.response.prompt)}</div>${scale(activity.response.scale_labels,s.revised)}<div class="actions"><button id="results" class="button primary" ${s.revised?'':'disabled'}>See class overview</button></div>`);document.querySelectorAll('[data-v]').forEach(b=>b.onclick=()=>{s.revised=+b.dataset.v;render()});document.querySelector('#results').onclick=async()=>{s.result=await api(`/api/dei/sessions/${sessionId}/revised`,{method:'POST',body:JSON.stringify({participant_token:pt,response:s.revised})});s.step=4;render()}}else resultsView(s.result,activity,sessionId,pt,s.initial,s.revised)};render()}
function resultsView(payload,activity,sessionId,pt,initial,revised){let focus=null;const draw=()=>{const r=focus&&payload.focus?payload.focus.movement_percentages:payload.results.percentages;page(`<div class="eyebrow">Class overview</div><h1>${esc(activity.results.heading)}</h1>${activity.results.intro?`<p class="muted">${esc(activity.results.intro)}</p>`:''}<div class="own"><div>Started<br><strong>${esc(activity.response.scale_labels[initial-1])}</strong></div><div>→</div><div>Now<br><strong>${esc(activity.response.scale_labels[revised-1])}</strong></div></div>${flow(payload.results.matrix,activity.response.scale_labels,focus)}<div class="filters">${activity.response.scale_labels.map((l,i)=>`<button class="filter ${focus===i+1?'active':''}" data-f="${i+1}">Started: ${esc(l)}</button>`).join('')}<button class="filter" data-f="0">Whole class</button></div><div class="stats"><div class="stat"><strong>${r.lower}%</strong><br>Moved lower</div><div class="stat"><strong>${r.unchanged}%</strong><br>Unchanged</div><div class="stat"><strong>${r.higher}%</strong><br>Moved higher</div></div>${activity.results.discussion_prompts?.length?`<div class="questions"><h3>Questions to consider</h3><ul>${activity.results.discussion_prompts.map(x=>`<li>${esc(x)}</li>`).join('')}</ul></div>`:''}`);document.querySelectorAll('[data-f]').forEach(b=>b.onclick=async()=>{focus=+b.dataset.f||null;payload=await api(`/api/dei/sessions/${sessionId}/results?participant_token=${encodeURIComponent(pt)}${focus?`&focus=${focus}`:''}`);draw()})};draw()}
async function lecturer(activityId,params){let key=sessionStorage.getItem('dei-lecturer-key')||'';if(!key){page(`<div class="eyebrow">Lecturer</div><h1>Open this activity</h1><input id="key" type="password"><button id="go" class="button primary">Continue</button>`);document.querySelector('#go').onclick=()=>{sessionStorage.setItem('dei-lecturer-key',document.querySelector('#key').value);lecturer(activityId,params)};return}let sessionId=params.get('session');if(!sessionId){const o=await api(`/api/dei/activities/${activityId}/open-session`);sessionId=o.session_id;if(!sessionId)sessionId=(await api(`/api/dei/activities/${activityId}/sessions`,{method:'POST',headers:{'x-dei-lecturer-key':key},body:'{}'})).session_id}const refresh=async()=>{const d=await api(`/api/dei/sessions/${sessionId}/lecturer`,{headers:{'x-dei-lecturer-key':key}});page(`<div class="eyebrow">Lecturer</div><h1>${esc(activityId)}</h1><div class="lecturer-grid"><div class="stat"><span class="big">${d.counts.initial}</span><br>Initial responses</div><div class="stat"><span class="big">${d.counts.reviewed}</span><br>Reviewed information</div><div class="stat"><span class="big">${d.counts.revised}</span><br>Revised responses</div></div><div class="actions"><button id="display" class="button">Open presentation</button><button id="refresh" class="button">Refresh</button><button id="close" class="button primary">End session</button></div>`);document.querySelector('#display').onclick=()=>window.open(`/#/display/${activityId}?session=${sessionId}`,'_blank');document.querySelector('#refresh').onclick=refresh;document.querySelector('#close').onclick=async()=>{await api(`/api/dei/sessions/${sessionId}/close`,{method:'POST',headers:{'x-dei-lecturer-key':key},body:'{}'});await refresh()}};await refresh()}
async function presentation(activityId,params){const sessionId=await resolveSession(activityId,params.get('session'));const refresh=async()=>{const d=await api(`/api/dei/sessions/${sessionId}/presentation`);page(`<div class="eyebrow">Class overview</div><div class="prompt">${esc(d.prompt)}</div>${d.results.total?`<h1>${esc(d.results_copy.heading)}</h1>${flow(d.results.matrix,d.scale_labels)}<div class="stats"><div class="stat"><strong>${d.results.percentages.lower}%</strong><br>Moved lower</div><div class="stat"><strong>${d.results.percentages.unchanged}%</strong><br>Unchanged</div><div class="stat"><strong>${d.results.percentages.higher}%</strong><br>Moved higher</div></div>`:`<div class="panel"><span class="big">${d.counts.revised}</span><br>Revised responses received</div>`}<button id="refresh" class="button">Refresh</button>`,'presentation');document.querySelector('#refresh').onclick=refresh};await refresh()}
async function main(){try{const{parts,params}=route();if(parts[0]==='respond'&&parts[1])return student(parts[1],params);if(parts[0]==='control'&&parts[1])return lecturer(parts[1],params);if(parts[0]==='display'&&parts[1])return presentation(parts[1],params);page(`<div class="eyebrow">DEI Engine</div><h1>Choose a configured activity surface</h1>`)}catch(e){page(`<h1>Something went wrong</h1><p>${esc(e.message)}</p>`)}}window.addEventListener('hashchange',main);main();
